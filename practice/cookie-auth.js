/**
 * The user provides credentials which are compared to the server.
 * If it fails redirect to the login page with message.
 * 
 * If validation succeeds set cookie on the users browser by using a 
 * set-cookie header.
 */

 